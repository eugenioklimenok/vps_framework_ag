"""
HOST Init — Controlled host reconciliation (current slice: user + SSH access).

This package contains:
    - User reconciliation logic
    - Filesystem reconciliation logic (home, .ssh, authorized_keys)
    - Post-action validation
    - Init runner orchestration
"""
